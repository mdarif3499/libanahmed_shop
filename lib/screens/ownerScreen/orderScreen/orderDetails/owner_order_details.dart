import 'package:flutter/material.dart';

class OwnerOrderDetails extends StatelessWidget {
  const OwnerOrderDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Owner Order Detials"),
    );
  }
}